export enum Scalar {
    STRING = 'string',
    NUMBER = 'number',
    INTEGER = 'integer',
    OBJECT = 'object',
    ARRAY = 'array',
    BOOLEAN = 'boolean',
    NULL = 'null',
}

export enum StringFormat {
    SHORT = 'short',
    LONG = 'long',
    EMAIL = 'email',
    URI = 'uri',
    UUID = 'uuid',
    DATE = 'date',
    DATE_TIME = 'date-time',
    TIME = 'time',
    PASSWORD = 'password',
    HTML = 'html',
    MARKDOWN = 'markdown',
}

export enum NumberFormat {
    FLOAT = 'float',
    DOUBLE = 'double',
    NUMERIC = 'numeric',
    CURRENCY = 'currency',
}

export enum BooleanFormat {
    BOOLEAN = 'boolean',
}
