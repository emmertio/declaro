import { describe, it } from 'vitest'

describe('Models', () => {
    it('should define a model', () => {
        // const model = defineModel({
        //     name: 'User',
        //     properties: {
        //         id: { type: 'string' },
        //         name: { type: 'string' },
        //     }
        // })
    })
})
