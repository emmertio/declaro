export enum Scalar {
    String = 'string',
    Number = 'number',
    Boolean = 'boolean',
    Date = 'date',
    Relation = 'relation',
    Embedded = 'embedded',
    JSON = 'json',
    Enum = 'enum',
}
